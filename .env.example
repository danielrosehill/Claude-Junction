# Agent Junction Configuration

# Bind address: 127.0.0.1 = localhost only, 0.0.0.0 = LAN accessible
JUNCTION_HOST=127.0.0.1
JUNCTION_PORT=4200

# Session management
JUNCTION_SESSION_TIMEOUT_MS=1800000
JUNCTION_SWEEP_INTERVAL_MS=60000

# Known LAN hosts (comma-separated name=ip or name=ip:port)
# Agents can query these with the known_hosts tool
# Example: JUNCTION_KNOWN_HOSTS=workstation=10.0.0.6,vm=10.0.0.4,nas=10.0.0.50:4200
JUNCTION_KNOWN_HOSTS=
